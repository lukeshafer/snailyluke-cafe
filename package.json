{
  "name": "snailyluke-cafe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "overlay:dev": "pnpm run --filter overlay dev",
    "overlay:dev:host": "pnpm run --filter overlay dev:host",
    "overlay:build": "pnpm run --filter overlay build",
    "site:dev": "pnpm run --filter site dev"
  },
  "devDependencies": {
    "@pulumi/pulumi": "^3.111.1",
    "@tsconfig/node18": "^18.2.2",
    "aws-cdk-lib": "2.110.1",
    "constructs": "10.3.0",
    "prettier": "^3.2.4",
    "sst": "^3.0.2",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
